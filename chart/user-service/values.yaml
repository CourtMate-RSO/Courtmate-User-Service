replicaCount: 1

image:
  repository: courtmate603fc8acr.azurecr.io/user-service
  tag: dev
  pullPolicy: IfNotPresent

# Container port
containerPort: 8080

# Service configuration
service:
  type: ClusterIP
  port: 80

# Ingress configuration
ingress:
  enabled: true
  className: nginx
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
  host: "courtmate.duckdns.org"
  path: /api/users(/|$)(.*)
  tls:
    enabled: true
    secretName: user-service-tls

# Health check probes
probes:
  enabled: true
  path: /health
  liveness:
    initialDelaySeconds: 30
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 3
  readiness:
    initialDelaySeconds: 10
    periodSeconds: 5
    timeoutSeconds: 3
    failureThreshold: 3

# Resource limits and requests
resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

# Environment variables from secret
envFromSecret:
  enabled: true
  secretName: user-service-secrets

# Additional environment variables
env:
  - name: ENV
    value: "prod"

# Override names
nameOverride: ""
fullnameOverride: ""
